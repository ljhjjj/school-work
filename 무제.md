대체로 **방향은 잘 됐어.**  
특히 핵심이었던 3가지는 제대로 들어갔어.

1. `단순 오타 예외`에서 에이전트가 새로 만든 이상 문자, 제어 문자, Markdown/JSON 파손을 제외하도록 바뀜.
    
2. `self-induced artifact corruption`이라는 범용 분류가 생김.
    
3. 도구를 바꿔도 같은 위치 수정/복구는 최대 2회로 계산한다는 규칙이 들어감.
    

다만 **그대로 쓰기에는 아직 3개 문제가 있어.**

## 1. Markdown 코드블록 fence가 깨져 있음

`10.2`와 `11.1` 쪽에 닫는 코드블록이 ```` 로 되어 있어.  
예를 들면 `2회 실패 후 금지` 블록 끝이 네 개 백틱으로 닫혀 있고, `11.1 인라인 실행 금지 조건`의 금지 예시도 네 개 백틱으로 닫혀 있어.

이건 실제 에이전트가 AGENTS.md를 읽을 때 **이후 내용을 코드블록으로 오인하거나 구조를 잘못 해석할 위험**이 있어.

수정:

````

즉, 해당 위치의 ```` 를 ``` 로 바꾸면 돼.

## 2. `python -c` 금지와 `python -c` 예시가 충돌함

`11.1`에서는 여러 줄, 한글, 파일 수정, 인코딩 복구 등에서는 `python -c`, `powershell -Command`, `node -e`를 금지한다고 잘 적었어. :contentReference[oaicite:2]{index=2}

그런데 바로 뒤 `12.1`에서 한글 깨짐 방지 예시로 다시:

```bash
PYTHONIOENCODING=utf-8 python -c "..."
````

가 나와.

이건 에이전트가 “인코딩 문제는 python -c 써도 되나?”라고 헷갈릴 수 있어.  
권장 수정은 `12.1`의 Python 예시를 이렇게 바꾸는 것.

````md
Python 검증 스크립트 실행 시 한글 깨짐 방지:

```bash
PYTHONIOENCODING=utf-8 python temp_check.py
python -X utf8 temp_check.py
````

스크립트 내부:

```python
import sys
sys.stdout.reconfigure(encoding="utf-8")
```

주의:

- 파일 내용을 수정하거나 인코딩 복구를 수행하는 경우 `python -c`는 사용하지 않는다.
    
- 별도 `.py` 임시 파일을 UTF-8로 저장해 실행한다.
    


## 3. 섹션 경계 줄바꿈이 조금 불안정함

`10.3` 끝나자마자 바로 `## 11. 임시 스크립트 사용 기준`이 붙어 있어. :contentReference[oaicite:4]{index=4}  
Markdown 파서가 대부분 처리하긴 하지만, 가독성과 에이전트 해석 안정성을 위해 `## 11` 앞에 빈 줄과 `---`를 넣는 게 좋아.

수정:


같은 논리적 위치의 수정 또는 복구는 최대 2회까지만 허용한다.
도구가 달라져도 카운트는 초기화하지 않는다.

---

## 11. 임시 스크립트 사용 기준


`12.3` 앞도 `---` 바로 다음에 제목이 붙어 있어서, 빈 줄 하나를 넣는 편이 좋아.

## 판단

내용 설계는 **잘 됨**.  
하지만 Markdown 문법과 `python -c` 충돌 때문에, 지금 상태는 **“개념은 통과, 파일 품질은 추가 수정 필요”** 정도야.

최소 수정은 이 3개면 충분해.

```text
1. 10.2와 11.1의 잘못된 ```` 닫힘을 ```로 수정
2. 12.1의 python -c 예시 제거
3. 10.3 → 11, 12.2 → 12.3 사이에 구분선/빈 줄 정리
```

이것만 고치면 이번 65만 토큰 폭주 방지용 규칙으로는 꽤 괜찮아.